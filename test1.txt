




#include<stdio.h>
#include<stdlib.h>
struct Node
{
    int data ;
    struct Node *link;
};
int main ()
{
  struct Node *a=NULL;
  struct Node *s=NULL;
  struct Node *d=NULL;
  a=(struct Node*)malloc(sizeof(struct Node));
  s=malloc(sizeof(struct Node));
  d=malloc(sizeof(struct Node));
  a->data=10;
  s->data=20;
  d->data=30;
  a->link=s;
  s->link=d;
  d->link=NULL;
  while (a !=NULL)
  {
     printf("%d-< ",a->data);
     a=a->link;
  }
  printf("NULL\n");
}
